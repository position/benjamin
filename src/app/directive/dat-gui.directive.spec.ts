import { DatGuiDirective } from './dat-gui.directive';

describe('DatGuiDirective', () => {
  it('should create an instance', () => {
    const directive = new DatGuiDirective();
    expect(directive).toBeTruthy();
  });
});
